export interface GlobalSettingsResponse {
  insert_vehicle_global_settings_one: {
    id: string;
  };
}

export interface SpecificSettingsResponse {
  insert_vehicle_specific_settings_one: {
    id: string;
  };
}

export interface VehicleConfigurationResponse {
  insert_vehicle_configuration_one: {
    id: string;
  };
}
